import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.irregular_quiz.app',
  appName: 'Irregular Quiz',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
